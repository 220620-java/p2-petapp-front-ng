import { PetAgeColorDirective } from './pet-age-color.directive';

describe('PetAgeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PetAgeColorDirective();
    expect(directive).toBeTruthy();
  });
});
