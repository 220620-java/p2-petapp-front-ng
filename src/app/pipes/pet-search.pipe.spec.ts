import { PetSearchPipe } from './pet-search.pipe';

describe('PetSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PetSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
